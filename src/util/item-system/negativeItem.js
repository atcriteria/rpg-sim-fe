// This is the "out-of-bounds" item, which is returned
// in the event that a bad object path is given and we
// cannot find the intended object.

const negativeItem = {
    name: "Negative Item"
}

export default negativeItem;